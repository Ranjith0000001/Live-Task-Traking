{
  "name": "live-task-tracking",
  "version": "1.0.0",
  "description": "Live Task Tracking Application",
  "scripts": {
    "dev": "npx concurrently --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npx concurrently --kill-others \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
